#include "CPython.h"
#include <vector>
using namespace std;

int main() {

	//map<int, int> mii;
	//mii.insert({ 1, 5 });
	//mii.insert({ 3, 17 });
	//mii.insert({ 44, 55 });
	//
	//Print p(mii);

	Print::Settings()
	return 0;
}